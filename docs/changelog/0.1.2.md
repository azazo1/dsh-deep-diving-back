# dsh-deep-diving-back v0.1.2

Date: 2026-09-25

修正状态文案的还原度: 0.1.6 的 `深度求索中...` 末尾省略号, 在 0.1.7-alpha.1 与蓝色流光一起被去掉了, 现在补回来.

## Bug Fixes

- 中文界面下状态行显示 `深度求索中` 而不是 0.1.6 的 `深度求索中...`; 现在只在缺失时补回省略号, 英文串 `Deep diving...` 原样不动.
- 计时部分与 0.1.6 逐行比对一致: `formatRunDuration` 实现与 `duration.*` 模板都没有漂移, 本版没有改动.

## Upgrade Notes

1. 升级后重启宿主并刷新页面; 没有配置变更.
