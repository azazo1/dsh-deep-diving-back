# dsh-deep-diving-back v0.1.1

Date: 2026-09-25

本版只更新发布流程与市场元数据, 插件运行时行为与 0.1.0 完全相同.

## Release

- GitHub Release 附带无版本号的预构建 tarball (`dsh-deep-diving-back.tgz`), 市场条目用 `releases/latest/download` 引用它, 以后发版不需要再改条目.
- 发布改用 npm 受信发布 (OIDC), 不再依赖长期 token; 预发布版本发布到 `next` dist-tag.
- 仓库新增市场详情页的截图声明 (`screenshots.json`).

## Upgrade Notes

1. 不需要任何操作; 0.1.0 到 0.1.1 的插件代码没有变化.
